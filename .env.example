# NanoClaw Environment Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# AUTHENTICATION (Required - choose one)
# =============================================================================

# Claude Code OAuth token (recommended)
# Get this by running `claude auth` in terminal
#CLAUDE_CODE_OAUTH_TOKEN=your_oauth_token_here

# Or use Anthropic API key directly
#ANTHROPIC_API_KEY=your_api_key_here

# =============================================================================
# SDK BACKEND SELECTION
# =============================================================================

# Which SDK backend to use for agent execution
# Options: 'claude' | 'opencode'
# - claude: Use official Claude Agent SDK (@anthropic-ai/claude-agent-sdk)
# - opencode: Use OpenCode SDK (@opencode-ai/sdk) for alternative backends
# Default: claude
#NANOCLAW_SDK_BACKEND=claude

# =============================================================================
# CONTAINER CONFIGURATION
# =============================================================================

# Container image to use for agent execution
#CONTAINER_IMAGE=nanoclaw-agent:latest

# Timeout for container execution in milliseconds (default: 30 minutes)
#CONTAINER_TIMEOUT=1800000

# Maximum output size from container in bytes (default: 10MB)
#CONTAINER_MAX_OUTPUT_SIZE=10485760

# Maximum concurrent containers (default: 5)
#MAX_CONCURRENT_CONTAINERS=5

# Idle timeout - how long to keep container alive after last result (default: 30 minutes)
#IDLE_TIMEOUT=1800000

# =============================================================================
# AGENT CONFIGURATION
# =============================================================================

# Name the agent responds to (used in trigger pattern @Name)
#ASSISTANT_NAME=Andy

# =============================================================================
# LOGGING
# =============================================================================

# Log level: trace | debug | info | warn | error
#LOG_LEVEL=info

# Timezone for scheduled tasks (default: system timezone)
#TZ=America/New_York
